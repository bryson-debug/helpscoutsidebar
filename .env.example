# HelpScout Dynamic App (from HelpScout Developer / My Apps portal)
HELPSCOUT_APP_SECRET=
SESSION_TOKEN_SECRET=

# The app must only run on mailboxes it's explicitly enabled for. Multiple
# values are separated by `|` (not comma — mailbox names can contain commas,
# e.g. "That Music Teacher, LLC"). Prefer numeric mailbox IDs (stable) once
# known from a live request; names are a fallback for mailboxes whose ID
# hasn't been confirmed yet. Known ID so far: Tarbet Education Network = 364558.
ALLOWED_MAILBOX_NAME=Tarbet Education Network|That Music Teacher, LLC
ALLOWED_MAILBOX_ID=364558

# Flodesk API (read + write scopes)
FLODESK_API_KEY=

# Google Sheets audit log
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
GOOGLE_SHEET_ID=
